# Copy to .env.local; never commit real credentials.
# Only the publishable (or legacy anon) key belongs in NEXT_PUBLIC variables.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional future plant AI; server-only, never NEXT_PUBLIC.
# GEMINI_API_KEY=
# GEMINI_MODEL=
